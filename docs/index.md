# MagPlotter documentation index

This `docs` folder is a small, human-oriented entry point describing where to find key functionality in the repository.

- `README.md` (project root) — full quickstart and usage examples
- `sensor_lab/README.md` — core processing internals and API
- `src/README.md` — support utilities
- `scripts/README.md` — helper scripts

If you want a hosted documentation site, generate Markdown from these files and publish with GitHub Pages or MkDocs.
